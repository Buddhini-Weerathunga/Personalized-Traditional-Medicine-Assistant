// backend/src/services/ragService.js
// Placeholder: in future, implement vector search on Ayurveda PDFs.

async function retrieveRelevantContext(question) {
  // For now, no actual retrieval
  return "Basic Ayurvedic knowledge context (RAG placeholder).";
}

module.exports = { retrieveRelevantContext };
